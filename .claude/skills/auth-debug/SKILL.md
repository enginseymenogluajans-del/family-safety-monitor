# Auth Debug
1. Confirm which machine runs backend (Windows) vs frontend (Mac)
2. Check API keys match in both .env files
3. Verify default profile exists in store
4. Test endpoint with curl from the SAME machine as the client
