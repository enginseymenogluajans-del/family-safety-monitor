# Expo extra config — set these in app.json > extra or via EAS environment variables
# BACKEND_URL: Cloudflare tunnel or local network URL of the Python backend
# API_KEY: must match X-API-Key value set in backend/.env
BACKEND_URL=https://your-tunnel.trycloudflare.com
API_KEY=your-secret-api-key
